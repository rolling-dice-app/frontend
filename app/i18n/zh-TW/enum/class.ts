import { CLASS_LABELS } from '~/constants/dnd'
import { SUBCLASS_CONFIG } from '~/constants/subclass'

/** 職業相關 enum label：主職業 + 各職業流派 / 範型 */
export default {
  label: CLASS_LABELS,
  subclass: SUBCLASS_CONFIG,
}
